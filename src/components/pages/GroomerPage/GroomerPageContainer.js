import React from 'react';
import RenderGroomerPage from './RenderGroomerPage';

function GroomerPageContainer({ LoadingComponent }) {
  return (
    <>
      <RenderGroomerPage />
    </>
  );
}

export default GroomerPageContainer;
